package src

const UPSTREAM_DOCKERHUB = "https://registry-1.docker.io"

const HTTP_PORT_80 = "80"
const HTTP_PORT_443 = "443"

const HTTP_PROTO_HTTP = "http"
const HTTP_PROTO_HTTPS = "https"

const HTTP_HEADER_HOST = "Host"
const HTTP_HEADER_X_FORWARDED_HOST = "X-Forwarded-Host"
const HTTP_HEADER_X_FORWARDED_PROTO = "X-Forwarded-Proto"
const HTTP_HEADER_X_FORWARDED_PORT = "X-Forwarded-Port"
const HTTP_HEADER_WWW_AUTHENTICATE = "WWW-Authenticate"

const BEARER = "Bearer"
const REALM = "realm"
const SERVICE = "service"
const SCOPE = "scope"
